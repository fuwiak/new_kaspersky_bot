# 🚀 Развертывание на Railway - Пошаговая инструкция

## ✅ Что было исправлено

1. **Collector теперь использует переменную окружения для порта** - можно настроить через `COLLECTOR_PORT`
2. **Collector слушает на 0.0.0.0** - доступен внутри контейнера для server
3. **Улучшены логи запуска** - теперь видно, когда запускаются server и collector
4. **Обновлена документация** - добавлены подробные инструкции по решению проблем

## 📋 Быстрые шаги для развертывания

### 1. Подготовка кода

```bash
# Убедитесь, что все изменения закоммичены
git add .
git commit -m "Fix collector for Railway deployment"
git push
```

### 2. Создание проекта на Railway

1. Откройте [Railway Dashboard](https://railway.app/dashboard)
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите репозиторий `anything-llm-vlad`
5. Railway автоматически начнет сборку

### 3. Добавление PostgreSQL (ОБЯЗАТЕЛЬНО!)

1. В проекте Railway нажмите **"+ New"**
2. Выберите **"Database"** → **"PostgreSQL"**
3. Railway автоматически создаст `DATABASE_URL`

### 4. Настройка переменных окружения

В настройках вашего сервиса (Settings → Variables) добавьте:

#### Минимально необходимые:

```env
NODE_ENV=production
LLM_PROVIDER=openrouter
OPENROUTER_API_KEY=ваш-ключ
```

#### Рекомендуемые дополнительные:

```env
VECTOR_DB=lancedb
EMBEDDING_ENGINE=inherit
COLLECTOR_PORT=8888
DISABLE_TELEMETRY=true
```

### 5. Ожидание деплоя

Railway автоматически:
- ✅ Соберет Docker образ
- ✅ Выполнит миграции базы данных
- ✅ Запустит server и collector
- ✅ Предоставит публичный URL

### 6. Проверка работы

1. **Проверьте логи**:
   - В Railway Dashboard откройте ваш сервис
   - Перейдите в "Deployments" → последний деплой → "View Logs"
   - Должны быть сообщения:
     ```
     Starting server on port XXXX...
     Starting collector on port 8888...
     Document processor app listening on port 8888
     ```

2. **Проверьте доступность**:
   - Откройте URL, который предоставил Railway
   - Должен загрузиться интерфейс AnythingLLM

3. **Проверьте процессор документов**:
   - Попробуйте загрузить документ
   - Если видите ошибку "Процессор документов недоступен", см. раздел "Решение проблем"

## 🔧 Решение проблем

### Ошибка: "Процессор документов недоступен"

**Быстрое решение**:

1. Проверьте логи Railway - должно быть сообщение о запуске collector
2. Если collector не запустился:
   - Перезапустите деплой (Redeploy)
   - Проверьте, что `COLLECTOR_PORT=8888` установлен (или не установлен для значения по умолчанию)
3. Убедитесь, что оба процесса (server и collector) запускаются одновременно

**Подробные инструкции**: см. `RAILWAY_TROUBLESHOOTING.md`

### Другие проблемы

См. файл `RAILWAY_TROUBLESHOOTING.md` для подробных инструкций по решению проблем.

## 📚 Дополнительная документация

- `RAILWAY_QUICK_START.md` - подробная инструкция по развертыванию
- `RAILWAY_TROUBLESHOOTING.md` - решение проблем
- `RAILWAY_DEPLOY.md` - оригинальная документация

## ✅ Чеклист перед деплоем

- [ ] Код закоммичен и запушен в Git
- [ ] PostgreSQL база данных добавлена в Railway
- [ ] Переменные окружения настроены (минимум: `NODE_ENV`, `LLM_PROVIDER`, API ключ)
- [ ] Railway проект создан и подключен к репозиторию
- [ ] Готовы проверить логи после деплоя

## 🎉 Готово!

После успешного деплоя ваше приложение будет доступно по URL Railway. Все компоненты (server, collector, frontend) будут работать автоматически!
