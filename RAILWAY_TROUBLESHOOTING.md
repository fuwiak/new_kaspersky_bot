# 🔧 Решение проблем на Railway

## ❌ Ошибка: "Процессор документов недоступен"

Эта ошибка означает, что server не может подключиться к collector (обработчику документов).

### Диагностика

1. **Проверьте логи Railway**:
   ```bash
   # В Railway Dashboard:
   # 1. Откройте ваш проект
   # 2. Выберите сервис
   # 3. Перейдите в "Deployments"
   # 4. Выберите последний деплой
   # 5. Нажмите "View Logs"
   ```

2. **Что искать в логах**:
   - ✅ `"Starting server on port XXXX..."`
   - ✅ `"Starting collector on port 8888..."`
   - ✅ `"Document processor app listening on port 8888"`
   - ❌ Ошибки при запуске collector
   - ❌ `"EADDRINUSE"` - порт уже занят
   - ❌ `"ECONNREFUSED"` - не может подключиться

### Решения

#### Решение 1: Проверьте, что оба процесса запускаются

В логах должны быть сообщения о запуске обоих процессов:
```
Starting server on port 3001...
Starting collector on port 8888...
Document processor app listening on port 8888
```

Если collector не запускается:
- Проверьте ошибки в логах
- Убедитесь, что `COLLECTOR_PORT=8888` установлен (или не установлен для значения по умолчанию)

#### Решение 2: Перезапустите деплой

1. В Railway Dashboard откройте ваш сервис
2. Нажмите на три точки (⋯) рядом с деплоем
3. Выберите "Redeploy"

#### Решение 3: Проверьте переменные окружения

Убедитесь, что установлены:
```env
NODE_ENV=production
COLLECTOR_PORT=8888  # Опционально, по умолчанию 8888
```

#### Решение 4: Проверьте, что используется правильный Dockerfile

Убедитесь, что `railway.toml` указывает на правильный Dockerfile:
```toml
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
```

#### Решение 5: Проверьте docker-entrypoint.sh

Убедитесь, что `docker-entrypoint.sh` запускает оба процесса:
```bash
# Должно быть:
{
    node /app/server/index.js
} &
{ 
    node /app/collector/index.js
} &
```

## ❌ Ошибка: "DATABASE_URL is not set"

### Решение

1. Добавьте PostgreSQL сервис в Railway:
   - В Railway Dashboard нажмите "+ New"
   - Выберите "Database" → "PostgreSQL"
   - Railway автоматически установит `DATABASE_URL`

2. Проверьте переменные окружения:
   - Убедитесь, что `DATABASE_URL` виден в настройках сервиса
   - Если нет, перезапустите деплой

## ❌ Ошибка: Миграции базы данных не выполняются

### Решение

1. Проверьте логи при запуске:
   - Должно быть: `"Database setup completed successfully!"`
   - Если есть ошибки, они будут в логах

2. Проверьте подключение к базе данных:
   - Убедитесь, что PostgreSQL сервис работает
   - Проверьте `DATABASE_URL` в переменных окружения

3. Выполните миграции вручную (если нужно):
   ```bash
   railway run npx prisma db push --schema=./server/prisma/schema.prisma
   ```

## ❌ Ошибка: Приложение не запускается

### Решение

1. **Проверьте логи**:
   - Найдите первую ошибку в логах
   - Обычно это указывает на проблему

2. **Проверьте обязательные переменные**:
   ```env
   NODE_ENV=production
   DATABASE_URL=postgresql://...  # Должен быть установлен автоматически
   LLM_PROVIDER=openrouter  # Или другой провайдер
   OPENROUTER_API_KEY=...  # Или другой API ключ
   ```

3. **Проверьте версию Node.js**:
   - Должна быть >= 18
   - Railway использует версию из Dockerfile

## ✅ Проверка работоспособности

После деплоя проверьте:

1. **Server работает**:
   ```bash
   curl https://your-app.railway.app/api/ping
   # Должен вернуть: {"online":true}
   ```

2. **Collector работает** (внутри контейнера):
   - Проверьте логи - должно быть сообщение о запуске collector
   - Server должен успешно подключаться к collector

3. **Frontend доступен**:
   - Откройте URL Railway в браузере
   - Должен загрузиться интерфейс AnythingLLM

## 📞 Дополнительная помощь

Если проблема не решена:

1. Проверьте [Railway Documentation](https://docs.railway.app)
2. Проверьте [AnythingLLM Documentation](https://docs.anythingllm.com)
3. Проверьте логи Railway на наличие ошибок
4. Убедитесь, что все переменные окружения установлены правильно
