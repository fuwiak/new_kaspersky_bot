# 🔧 Исправление проблемы IPv6/IPv4 с Collector

## ❌ Проблема

Ошибка: `connect ECONNREFUSED ::1:8888`

**Причина:**
- `localhost` резолвится в IPv6 адрес `::1` в некоторых системах
- Collector слушает на `0.0.0.0:8888` (IPv4)
- Server пытается подключиться к `::1:8888` (IPv6), но collector не слушает на IPv6

## ✅ Решение

Использовать `127.0.0.1` (IPv4) вместо `localhost` для явного указания IPv4 адреса.

### Изменения:

1. **server/utils/collectorApi/index.js**:
   - Изменено с `localhost` на `127.0.0.1` для Docker/production
   - Это гарантирует использование IPv4

2. **docker/docker-entrypoint.sh**:
   - Изменено с `localhost` на `127.0.0.1` для проверки готовности
   - Добавлена проверка IPv6 для диагностики

## 📋 Что было исправлено

### До:
```javascript
const collectorHost = "localhost";  // Может резолвиться в ::1 (IPv6)
this.endpoint = `http://localhost:8888`;
```

### После:
```javascript
const collectorHost = "127.0.0.1";  // Явно IPv4
this.endpoint = `http://127.0.0.1:8888`;
```

## 🚀 Развертывание

1. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Fix IPv6/IPv4 issue: use 127.0.0.1 instead of localhost"
   git push
   ```

2. Перезапустите деплой на Railway

3. Проверьте логи:
   - Должно быть: `[CollectorApi] Endpoint: http://127.0.0.1:8888`
   - Должно быть: `[CollectorApi] ✓ Collector is online and responding`

## ✅ Ожидаемый результат

После исправления:
- ✅ Server подключается к collector по `127.0.0.1:8888` (IPv4)
- ✅ Нет ошибок `ECONNREFUSED ::1:8888`
- ✅ Health check проходит успешно
- ✅ Загрузка документов работает

## 🔍 Диагностика

Если проблема сохраняется:

1. Проверьте логи `[CollectorApi]`:
   - Endpoint должен быть `http://127.0.0.1:8888`
   - Не должно быть ошибок `ECONNREFUSED ::1`

2. Проверьте логи `[ENTRYPOINT]`:
   - Должно быть: `✓ Collector HTTP endpoint responded successfully on 127.0.0.1:8888`

3. Проверьте логи `[COLLECTOR]`:
   - Должно быть: `✓ Server listening on 0.0.0.0:8888`
   - Collector слушает на `0.0.0.0` (все интерфейсы), что правильно

## 📝 Примечания

- `0.0.0.0` используется для **прослушивания** (bind) - слушает на всех интерфейсах
- `127.0.0.1` используется для **подключения** (connect) - явно IPv4
- Это стандартная практика для избежания проблем с IPv6/IPv4
