# 🚀 Быстрый старт на Railway

## 📋 Что будет запущено

При развертывании на Railway автоматически запускаются **все компоненты**:
- ✅ **Server** (API сервер) - на порту, который предоставляет Railway (переменная `PORT`)
- ✅ **Collector** (обработчик документов) - на порту 8888 внутри контейнера
- ✅ **Frontend** (веб-интерфейс) - встроен в server и обслуживается через него

Все компоненты работают в **одном Docker контейнере** и автоматически запускаются через `docker-entrypoint.sh`.

## 🎯 Шаги для развертывания

### 1. Подготовка репозитория

```bash
git add .
git commit -m "Ready for Railway deployment"
git push
```

### 2. Создание проекта на Railway

1. Войдите в [Railway Dashboard](https://railway.app/dashboard)
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите ваш репозиторий `anything-llm-vlad`

### 3. Добавление PostgreSQL базы данных (ОБЯЗАТЕЛЬНО!)

1. В Railway Dashboard откройте ваш проект
2. Нажмите **"+ New"** → выберите **"Database"** → выберите **"PostgreSQL"**
3. Railway автоматически создаст переменную окружения `DATABASE_URL`
4. Миграции Prisma будут выполнены автоматически при первом запуске

### 4. Настройка переменных окружения

В настройках вашего сервиса (Settings → Variables) добавьте:

#### Обязательные переменные:

```env
NODE_ENV=production
```

#### Настройки LLM (выберите один провайдер):

**OpenRouter:**
```env
LLM_PROVIDER=openrouter
OPENROUTER_API_KEY=ваш-ключ-от-openrouter
```

**OpenAI:**
```env
LLM_PROVIDER=openai
OPEN_AI_KEY=ваш-ключ-от-openai
```

**Anthropic:**
```env
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=ваш-ключ-от-anthropic
```

#### Дополнительные настройки:

```env
# Векторная база данных (по умолчанию LanceDB)
VECTOR_DB=lancedb

# Движок эмбеддингов (по умолчанию inherit)
EMBEDDING_ENGINE=inherit

# Отключить телеметрию (опционально)
DISABLE_TELEMETRY=true

# Порт для collector (по умолчанию 8888, обычно не нужно менять)
# Важно: collector должен работать на порту 8888 внутри контейнера
# Server подключается к collector через localhost:8888
COLLECTOR_PORT=8888
```

### 5. Настройка хранилища (опционально, но рекомендуется)

Railway предоставляет постоянное хранилище. Убедитесь, что переменная `STORAGE_DIR` установлена:

```env
STORAGE_DIR=/app/server/storage
```

Или Railway может использовать volume для постоянного хранения данных.

### 6. Деплой

Railway автоматически:
1. ✅ Обнаружит `Dockerfile` в корне проекта
2. ✅ Соберет Docker образ
3. ✅ Запустит контейнер с server и collector
4. ✅ Выполнит миграции базы данных
5. ✅ Предоставит публичный URL

### 7. Проверка работы

После деплоя:
1. Railway предоставит URL вашего приложения (например: `https://your-app.railway.app`)
2. Откройте этот URL в браузере
3. Вы должны увидеть веб-интерфейс AnythingLLM

## 🔍 Проверка статуса компонентов

После деплоя все компоненты должны работать:

- **Server**: доступен через публичный URL Railway
- **Collector**: работает внутри контейнера на порту 8888, доступен для server через `localhost:8888`
- **Frontend**: обслуживается через server на том же URL

## 🐛 Решение проблем

### Ошибка "DATABASE_URL is not set"

**Решение**: Убедитесь, что вы добавили PostgreSQL сервис в проект Railway. Railway автоматически установит `DATABASE_URL`.

### Ошибка "Процессор документов недоступен" / "Document processing API is not online"

**Решение**:
1. **Проверьте логи в Railway Dashboard**:
   - Откройте ваш сервис в Railway Dashboard
   - Перейдите на вкладку "Deployments" → выберите последний деплой → "View Logs"
   - Ищите сообщение: `"Document processor app listening on port 8888"` или `"Starting collector on port 8888..."`

2. **Убедитесь, что collector запустился**:
   - В логах должно быть два сообщения:
     - `"Starting server on port XXXX..."`
     - `"Starting collector on port 8888..."`
   - Если collector не запустился, проверьте ошибки в логах

3. **Проверьте переменные окружения**:
   - `COLLECTOR_PORT=8888` (опционально, по умолчанию 8888)
   - Убедитесь, что collector и server работают в одном контейнере

4. **Если проблема сохраняется**:
   - Перезапустите деплой в Railway
   - Проверьте, что оба процесса (server и collector) запускаются одновременно
   - Убедитесь, что нет конфликтов портов

### Ошибки при миграции базы данных

**Решение**:
- Проверьте, что PostgreSQL сервис добавлен и работает
- Проверьте логи в Railway Dashboard - там будут детали ошибок миграции
- Убедитесь, что `DATABASE_URL` правильно установлен

### Приложение не запускается

**Решение**:
1. Проверьте логи в Railway Dashboard
2. Убедитесь, что все обязательные переменные окружения установлены
3. Проверьте, что PostgreSQL сервис работает
4. Убедитесь, что LLM провайдер настроен правильно

## 📝 Важные замечания

1. **Один контейнер = все компоненты**: Server, Collector и Frontend работают в одном контейнере
2. **Порты**: Railway автоматически устанавливает `PORT` для server. Collector работает на 8888 внутри контейнера
3. **База данных**: PostgreSQL обязательна! SQLite не рекомендуется для production на Railway
4. **Хранилище**: Данные документов хранятся в `/app/server/storage` внутри контейнера. Для постоянного хранения используйте Railway Volumes

## 🎉 Готово!

После успешного деплоя ваше приложение будет доступно по URL, который предоставит Railway. Все компоненты (server, collector, frontend) будут работать автоматически!
